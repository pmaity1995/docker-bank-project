apiVersion: v1
kind: Service

metadata:
  name: account-service
  namespace: bank-app

spec:
  type: NodePort

  selector:
    app: account-service

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30007
